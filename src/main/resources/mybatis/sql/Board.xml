<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="me.xbb123.mvc.repository.BoardRepository">

	<select id="getList" parameterType="me.xbb123.mvc.domain.Board" resultType="me.xbb123.mvc.domain.Board">
		SELECT
			B.BOARD_SEQ,
			B.BOARD_TYPE,
			B.TITLE,
			B.CONTENTS,
			B.REG_DATE
		FROM T_BOARD B
		ORDER BY B.REG_DATE DESC		
	</select>
	
	<select id="get" parameterType="int" resultType="me.xbb123.mvc.domain.Board">
		SELECT
			B.BOARD_SEQ,
			B.BOARD_TYPE,
			B.TITLE,
			B.CONTENTS,
			B.REG_DATE
		FROM T_BOARD B
		WHERE B.BOARD_SEQ = #{boardSeq}			
	</select>
	
	<insert id="save" parameterType="me.xbb123.mvc.parameter.BoardParameter" useGeneratedKeys="true" keyProperty="boardSeq">
		INSERT INTO T_BOARD
		(
			BOARD_TYPE,
			TITLE,
			CONTENTS,
			REG_DATE		
		)
		VALUES
		(
			#{boardType.code},
			#{title},
			#{contents},
			NOW()
		)					
	</insert>
	
	<update id="update" parameterType="me.xbb123.mvc.parameter.BoardParameter">
		UPDATE T_BOARD
		SET 
			TITLE = #{title},
			CONTENTS = #{contents}
		WHERE BOARD_SEQ = #{boardSeq}					
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE FROM T_BOARD	
		WHERE BOARD_SEQ = #{boardSeq}					
	</delete>
	
	<insert id="saveList" parameterType="map">
		INSERT INTO T_BOARD
		(
			TITLE,
			CONTENTS,
			REG_DATE
		)
		VALUES
		<foreach collection="boardList" item="board" separator=",">
		(
			#{board.title},
			#{board.contents},
			NOW()
		)
		</foreach>
	</insert>
	
</mapper>